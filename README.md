# 0.2_weather_location

> **Note:** A VPN must be disabled for the weather location to work correctly. Ctrl+C closes the program.

# Windows
1. Clone the repository
2. Unzip the file
3. Open "main.py" in the project folder
4. Run `pip install -r requirements.txt` in the terminal
5. Run "main.py"
